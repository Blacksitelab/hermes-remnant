name: remnant
version: 0.2.1
description: "Durable memory engine for Hermes Agent: SQLite-backed fact storage, async LLM extraction, BM25 keyword search, and dedup with cosine similarity."
provides_hooks:
  - system_prompt_block
  - sync_turn
  - prefetch
  - queue_prefetch
  - shutdown
  - on_session_switch
  - on_session_end
  - on_pre_compress
  - on_memory_write
  - on_delegation
  - backup_paths
